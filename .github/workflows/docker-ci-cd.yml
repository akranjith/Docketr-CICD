name: Docker CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.akranjith108 }}
          password: ${{ secrets.dckr_pat_z8RNwIK2ZB1GhJjF34yj8OhHaf0 }}

      - name: Build Docker image
        run: |
          docker build -t ${{ secrets.akranjith108}}/my-app:latest .

      - name: Push Docker image
        run: |
          docker push ${{ secrets.akranjith108}}/my-app:latest
